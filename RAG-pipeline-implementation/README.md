# Multimodal RAG System

## Setup

```bash
# 1. Clone repo
git clone <your-url> && cd my_rag_project

# 2. Create environment
conda env create -f environment.yml
conda activate rag_env

# (or) pip install
pip install -r requirements.txt
